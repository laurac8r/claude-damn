projects/
backups/
cost-log/
cache/
file-history/
ide/
paste-cache/
plugins/
serena-dashboard-static/
session-env/
sessions/
tasks/
shell-snapshots/
telemetry/
security_warnings*.json
history.jsonl

settings.json
!settings
!tests/fixtures/settings.json

__pycache__/
*.py[cod]
.venv/
*.egg-info/

.config
debug/
image-cache/

usage-data/facets
usage-data/session-meta

# Raw subagent output
eval-compose/

# Docs generated by `superpowers` plugin / skill
docs/superpowers

# Claude localdev
.claude/*
# ...but track `hookify` rules
!.claude/hookify.*.md

# Worktrees
.credentials.json
.worktrees/
worktrees/

# Checkpointing + memory
.checkpoints/
.remember
CHECKPOINT.md

# Brainstorm / visual-companion scratch (SPA state + content)
.superpowers/

daemon/
jobs/

mcp-needs-auth-cache.json
daemon-auth-cooldown
daemon-auth-status.json
.last-update-result.json
.last-cleanup
security/
stats-cache.json
daemon.lock
daemon.status.json

# NOTE to `/sync` Skill -> NEVER delete the below line(s)
rules/PERSONALIZATION.md
