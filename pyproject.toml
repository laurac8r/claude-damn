[project]
name = "claude-damn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["pytest>=9.0.2"]

[dependency-groups]
dev = ["pyyaml>=6.0.3", "pytest-mock>=3.14"]

[tool.pytest.ini_options]
pythonpath = ["."]
